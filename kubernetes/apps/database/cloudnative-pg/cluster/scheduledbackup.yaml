---
# yaml-language-server: $schema=https://kubernetes-schemas.toskbot.xyz/postgresql.cnpg.io/scheduledbackup_v1.json
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: postgres
spec:
  schedule: "52 * * * *"
  immediate: true
  backupOwnerReference: self
  cluster:
    name: postgres
